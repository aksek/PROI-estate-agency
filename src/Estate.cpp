#include <iostream>
#include <string>
#include "Owner.h"
#include "Estate.h"

using namespace std;

/*template <typename priceType>
string Estate<priceType>::fullInfo()
{
    return name + " distr:" + district + " age:" + to_string(age) + " price:" + to_string(price) + " owner:" + owner->showName();
}

template <typename priceType>
void Estate<priceType>::feedData(std::string nameFeed, std::string districtFeed, unsigned ageFeed, int priceFeed, Owner *ownerFeed)
{
    name = nameFeed;
    district = districtFeed;
    age = ageFeed;
    price = priceFeed;
    owner = ownerFeed;
}

template <typename priceType>
void Estate<priceType>::feedData(){}

template <typename priceType>
Estate<priceType>::Estate(std::string nameFeed, std::string districtFeed, unsigned ageFeed, int priceFeed, Owner *ownerFeed)
{
    feedData(nameFeed, districtFeed, ageFeed, priceFeed, ownerFeed);
}*/
